import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.slidegenius.ai',
  appName: 'SlideGenius AI',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
